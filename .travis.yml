language: ruby
rvm:
  - 2.2.0
script:
  - bundle exec rake db:test:prepare
  - bundle exec rspec spec/
before_install:
  - gem update --system
  - gem install bundler
  - gem install rails
bundler_args: --binstubs=./bundler_stubs
